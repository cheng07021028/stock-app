name: Force Streamlit auth lock v76 DISABLED
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "v76 auto patch disabled. Use v78 fixed files only."
