name: Apply v73 factor fields patch

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  apply-v73-factor-fields:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Apply v73 patch
        run: python apply_v73_factor_fields_patch.py

      - name: Verify patch script syntax
        run: |
          python -m py_compile godpick_factor_schema.py
          python -m py_compile apply_v73_factor_fields_patch.py

      - name: Commit v73 factor fields
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add godpick_factor_schema.py apply_v73_factor_fields_patch.py pages/*.py godpick_record_service.py || true
          if git diff --cached --quiet; then
            echo "No changes to commit."
          else
            git commit -m "Fix v72 factor fields patch syntax and apply v73"
            git push
          fi
